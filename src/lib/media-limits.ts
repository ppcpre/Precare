/**
 * เพดานของไฟล์สื่อ — **แหล่งความจริงเดียว** ทั้งฝั่ง server และ client
 *
 * ไฟล์นี้ไม่ import อะไรเลยโดยตั้งใจ ทั้ง `storage.ts` (ซึ่งลาก drizzle
 * กับ schema มาด้วย จึงเข้า bundle ฝั่ง client ไม่ได้) และหน้าเลือกไฟล์
 * ต้องใช้ตัวเลขชุดเดียวกัน — เดิมฝั่ง client ประกาศ 40 MB ซ้ำไว้เอง
 * ซึ่งแปลว่าวันหนึ่งจะแก้ที่เดียวแล้วสองฝั่งไม่ตรงกัน
 */

/**
 * ⚠️ **500 MB × 10 คลิป = เต็มโควตารวมของทั้งแอป (5 GB)**
 *
 * เดิม 40 MB ได้ 128 คลิป ตัวเลขใหม่ทำให้ครอบครัวเดียวกินพื้นที่ของทุกคน
 * ได้ภายในสิบคลิป โควตารายครอบครัวจึงเปลี่ยนจาก "ควรมี" เป็น "ต้องมี"
 * ก่อนเปิดให้คนนอกใช้
 */
export const MAX_VIDEO_BYTES = 500 * 1024 ** 2;

/** ความยาวสูงสุดต่อคลิป — คุมปริมาณคู่กับขนาด เพราะวิดีโอย่อไม่ได้ */
export const MAX_VIDEO_MS = 30_000;

/**
 * ขนาดชิ้นละ 25 MB — ไฟล์ใหญ่ต้องส่งเป็นหลายคำขอ
 *
 * Cloudflare จำกัด **body ต่อหนึ่งคำขอไว้ที่ 100 MB** บนแพลนฟรีและ Pro
 * (Business 200 MB, Enterprise ถึง 5 GB) คำขอที่เกินถูกตอบ 413 ที่ขอบ
 * ตั้งแต่ยังไม่ถึง worker ของเรา — ไม่ว่าโค้ดจะเขียนดีแค่ไหนก็ไม่ช่วย
 * https://developers.cloudflare.com/workers/platform/limits/
 *
 * 500 MB จึงส่งเป็นคำขอเดียวไม่ได้ ต้องใช้ multipart ของ R2
 * เลือก 25 MB เพราะเหลือระยะห่างจากเพดานเยอะ (เผื่อ overhead ของ TLS
 * และ proxy) และยังมากกว่าขั้นต่ำของ R2 ที่ 5 MiB ต่อชิ้น
 * 500 MB จึงเป็น 20 ชิ้น ห่างจากเพดาน 10,000 ชิ้นของ R2 มาก
 */
export const VIDEO_PART_BYTES = 25 * 1024 ** 2;
